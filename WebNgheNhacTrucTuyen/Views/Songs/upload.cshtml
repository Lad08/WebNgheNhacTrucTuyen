@model WebNgheNhacTrucTuyen.Models.Songs
@{
    ViewData["Title"] = "Upload Bài Hát";
}

<h2>Upload Bài Hát</h2>

<form asp-action="Upload" method="post" enctype="multipart/form-data">
    <div>
        <label>Title:</label>
        <input type="text" name="title" required />
    </div>
    <div>
        <label>Artist:</label>
        <input type="text" name="artist" required />
    </div>
    <div class="form-group">
        <label asp-for="GenreId" class="control-label"></label>
        <select asp-for="GenreId" class="form-control" asp-items="@(new SelectList(ViewBag.Genres, "G_Id", "G_Name"))"></select>
        <span asp-validation-for="GenreId" class="text-danger"></span>
    </div>
    <div>
        <label>File:</label>
        <input type="file" name="file" accept="audio/*" required />
    </div>
    <div>
        <label>Cover Image:</label>
        <input type="file" name="coverImage" accept="image/*" required />
    </div>
    <button type="submit">Upload</button>
</form>
